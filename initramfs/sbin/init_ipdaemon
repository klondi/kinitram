#!/bin/bb
#TODO: avoid race conditions and similar stuff
. /etc/initram.conf

if [ "x$ipdaemonurl" != "x" ]; then
	(
		while true; do
		wget "$ipdaemonurl" -q -O /dev/null;
		sleep 300;
		done
	) &
	echo $! > /var/run/ipdaemon.pid
fi
